/**
 * 📷 Параметры камеры, вынесенные из `main.ts` для удобства тюнинга.
 *
 * Применяются в `setupCamera()` как дефолты, но могут быть переопределены
 * после создания `CameraComponent` (например, per-level настройки).
 */
export const CAMERA_DEFAULTS = {
  /** 📐 Минимальный зум (камера максимально далеко). */
  minZoom: 2,

  /** 📐 Максимальный зум (камера максимально близко). */
  maxZoom: 4,

  /** 🔍 Стартовый зум, если карта больше вьюпорта (читаемый минимум). */
  initialZoomFallback: 3,

  /** 🛡️ Допустимый rubber-band overshoot за `[minZoom, maxZoom]`.
   *  0.1 = CoC-feel, можно уйти на ±10% за лимиты до того как пружина вернёт. */
  zoomOvershoot: 0.1,

  /** ⏱️ Сколько секунд камера "прилипает" в overshoot без скролла. */
  zoomHoldTime: 0.5,

  /** 🧈 Жёсткость пружины зума (возврат к `zoomRest`). */
  zoomSpringK: 80,

  /** 🛑 Демпфирование пружины (меньше колебаний). */
  zoomDamping: 28,

  /** 🖱️ Чувствительность колеса мыши. */
  zoomSensitivity: 0.0012,

  /** 🧈 Коэффициент плавности follow (пока не используется — follow = snap). */
  smoothing: 8,
} as const;

/**
 * 🎮 Скорость игрока (px/sec). Вынесено сюда потому что `PlayerControlSystem`
 * сейчас принимает её параметром в `main.ts`, но это magic number.
 */
export const PLAYER_SPEED = 140;
