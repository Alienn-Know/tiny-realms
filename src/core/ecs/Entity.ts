/**
 * 🆔 Идентификатор сущности ECS.
 *
 * Просто число, без классов и обёрток. Дешёвый ключ для Map/Set и арифметика
 * для битмасок, если позже понадобится. Все компоненты привязываются к entity
 * по этому id в {@link World}.
 */
export type Entity = number;
